# Railway provides DATABASE_URL automatically when you add a Postgres plugin.
# For local development you can leave this unset — the app falls back to a local
# SQLite file (backend/media_tracking.db).
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Railway sets PORT automatically. Only needed if running the container manually.
PORT=8000
